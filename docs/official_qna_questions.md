# 공식 Q&A / 디스코드에서 확인할 질문 목록

> 채널: AI페스티벌 홈페이지 '공지사항 Q&A'탭, 디스코드(https://discord.gg/tVHYjJVh3f). 첫 회의 후 담당자를 정해 08.06 오프라인 설명회 Q&A 시간 또는 디스코드에 질문한다.

## 데이터/코퍼스 관련

1. 현재 보유한 `manifest.jsonl`(4,204건)·`universe.csv`(70개사)가 실제 평가 시 사용되는 코퍼스와 **완전히 동일**한가, 아니면 평가 시점에 갱신/추가될 수 있는가?
2. `file_format=pdf+html`인 3건(KB금융/한화오션/한화에어로스페이스)에 대해 평가 질의가 나올 가능성이 있는가? 있다면 PDF 파싱도 평가 범위에 포함되는가, 아니면 "확인 불가"로 응답해도 무방한가?
3. 실제 평가 질의(비공개)가 참고용 질의 6종의 유형·난이도 범위를 벗어날 가능성이 있는가(예: 지분공시 관련 질의도 포함되는지)? — **답변과 무관하게 우리는 전체 코퍼스를 최소 범용 RAG 지원 대상으로 이미 설계했다(`hybrid_architecture_proposal.md` §4). 이 질문은 확인용이지, 답에 따라 커버리지를 좁히기 위한 것이 아니다.**
4. `market_cap`(universe.csv, 2026-07-24 조회)처럼 "공시 시점과 다른" 참고용 필드가 평가 질의에서 직접 요구될 가능성이 있는가?
4-b. **[확인 기반] 참고 질의 Q4가 "유상증자, CB, BW, EB"를 명시하는데 제공 코퍼스에 BW(신주인수권부사채) 발행 공시는 0건이다.** 이런 경우 기대하는 답변은 "해당 공시가 없음"을 명시하는 것이 맞는가? (평가 정답이 "데이터 없음"인 문항이 실제로 포함되는지 확인)
4-c. **[확인 기반] 참고 질의 Q3의 "2차전지 A사·B사 설비투자 비교"에서, 거래소공시 신규시설투자등은 LG에너지솔루션·삼성SDI에 0건이고 정기공시 서술 섹션에만 설비투자 정보가 있다.** 이런 질의는 정기공시 본문 근거로 답하는 것을 의도한 것인가?
4-d. `major` 문서의 `doc_subtype`이 manifest에서 전부 빈 문자열인데, 이는 의도된 것인가 아니면 추후 채워질 예정인가?

## API/제출 관련

5. `GET /answer`의 `question_id`가 동일한 질의를 재요청할 수 있는가(캐싱/재현성 검증 목적인지)?
6. 서버 운영 필수 기간(09.07~09.20)이 "변경 시 별도 공지 예정"이라 했는데, 확정 일정이 사전에 공지되는가?
7. 응답 지연시간(latency)에 대한 정량적 기준(타임아웃 등)이 있는가?
8. `think_trace` 필드의 형식(문자열 vs JSON 객체)에 대한 세부 규격이 있는가, 아니면 자유 형식인가?
9. Private Repository 접근 권한(Github Organization 초대) 절차와 시점은 언제인가?

## 평가 관련

10. 정성평가의 "문제정의" 항목은 구체적으로 어떤 산출물(기술제안서? 코드? 둘 다?)을 근거로 평가되는가?
11. Closed 질의에서 정답이 여러 유효한 표현(예: "1,234억원" vs "123,400,000,000원")을 가질 때 채점 기준은 무엇인가?
12. "근거 공시 표시"의 형식에 권장 사항이 있는가(예: 공시명+공시일 텍스트면 충분한지, rcept_no 같은 식별자가 필요한지)?

## HyperCLOVA X 사용 관련

13. HyperCLOVA X API 사용량/비용에 대한 팀별 한도나 제공 크레딧이 있는가?
14. ~~"HyperCLOVA X만 사용 가능"의 범위 — 임베딩 모델도 HyperCLOVA X 계열로 한정되는가~~ **[확인 — 주최 측 공식 답변]** 답변 생성(LLM)은 HyperCLOVA X만 제한, **임베딩/검색 도구는 BGE-M3·multilingual-e5 등 사전학습 임베딩 모델 사용 허용**. 단 임베딩·검색 대상은 제공 코퍼스로 제한, 최종 답변 생성만 HyperCLOVA X. → `hybrid_architecture_proposal.md` §7.3, §8, `team_workstreams.md`, `development_roadmap.md`, `corpus_coverage_strategy.md`에 반영 완료.

## 우선순위

가장 답변 영향도가 큰 항목: **4-b, 4-c, 8**. (4-b/4-c는 실측으로 확인된 코퍼스-예시 불일치라 답변에 따라 설계가 달라진다. 8은 think_trace 규격.) **14는 확인됨(위 참조).** 그다음 **2, 3, 13**.

08.06 오프라인 설명회 전에 디스코드로 먼저 질문해 두고, 설명회 Q&A 시간에 재확인한다. **단, 답변이 늦거나 없어도 설계는 "전체 코퍼스 최소 RAG 커버리지" 원칙으로 진행한다** — 주최 측 답변을 기다리느라 일정을 멈추지 않는다.
