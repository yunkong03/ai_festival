# 재현성용 의존성 스냅샷 — pyproject.toml은 범위(>=)로만 고정되어 있어(byte reproducibility
# 보장 안 됨), 이 파일이 2026-08-04 전체 corpus 실행(corpus_snapshot_id=snap_7484a10220422056)
# 당시 실제 해석된 버전이다. Python 3.12.2에서 생성.
#
# 주의: 이 파일은 `pip freeze` 전체 출력이 아니다 — 실행 환경이 이 프로젝트 전용 가상환경이
# 아니라 여러 프로젝트가 공유하는 환경이라(주피터/pandas/sklearn 등 무관한 패키지 100여 개
# 섞여 있음), 실제로 이 프로젝트가 쓰는 패키지만 수동으로 골라 담았다(전이 의존성 포함).
#
# 설치: pip install -r requirements-lock.txt
beautifulsoup4==4.15.0
soupsieve==2.8.4
pytest==9.1.1
iniconfig==2.3.0
packaging==26.0
pluggy==1.6.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
attrs==26.1.0
referencing==0.37.0
rpds-py==2026.6.3

# tiktoken(pyproject.toml 선언, Fixed Token Chunker Phase용) — 이번 Workstream A 코드는
# 쓰지 않고, 현재 환경에도 설치되어 있지 않아 실측 버전을 적을 수 없다(B가 Chunking
# 구현 시점에 별도로 고정할 것).
